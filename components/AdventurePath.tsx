// AdventurePath — removed. The golden road SVG has been replaced by the
// per-section dotted milestone line in AdventureLand.tsx.
// This file is kept as a null export so any stale import won't break the build.

export default function AdventurePath() {
  return null;
}
